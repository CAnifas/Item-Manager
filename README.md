# Item-Manager
Item Manager — a lightweight Python desktop app to store, manage and search items by ID. Add items with custom icons, auto-generate or manually set IDs, and instantly look up any item. All data saved locally. Built with tkinter, no dependencies required.
